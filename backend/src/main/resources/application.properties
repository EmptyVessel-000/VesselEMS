# 应用名称
spring.application.name=VesselEMS

# 默认激活 dev 环境
spring.profiles.active=dev

spring.jpa.hibernate.ddl-auto=update

spring.security.filter.dispatcher-types=REQUEST

server.port=8080

spring.datasource.hikari.connection-test-query=SELECT 1
